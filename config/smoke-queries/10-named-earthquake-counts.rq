PREFIX jpe: <https://seismic.balog.jp/ontology/jp-earthquake.ttl#>
SELECT ?type (COUNT(?activity) AS ?count)
WHERE {
  GRAPH <https://seismic.balog.jp/graph/jma/named-earthquakes/20260831> {
    ?activity a jpe:NamedEarthquakeActivity ; jpe:activityType ?type .
  }
}
GROUP BY ?type
ORDER BY ?type
